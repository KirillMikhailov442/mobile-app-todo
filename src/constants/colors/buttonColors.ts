import colors from "./colors";

enum buttonColors {
    blackPrimary = colors.black2,
    blackLittleDark = colors.black3,

    grayPrimary = colors.gray1,
    grayLittleDark = colors.gray3,

    violetPrimary = colors.violet1,
    violetLittlLight = colors.violet2
}

export default buttonColors