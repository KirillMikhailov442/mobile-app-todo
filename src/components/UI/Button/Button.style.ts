import {StyleSheet} from 'react-native'

const ButtonStyles = StyleSheet.create({
    button: {
        borderColor: 'white',
        borderWidth: 1,
        paddingTop: 12,
        paddingBottom: 12,
        fontSize: 16,
        alignItems: 'center',
        height: 48,
    },
    buttonText: {
        fontSize: 16,
        fontWeight: '400'
    }
})

export default ButtonStyles