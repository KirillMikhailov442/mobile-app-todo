import {StyleSheet} from 'react-native'
import { TextStyles } from '../../../styles'

const ModalDeleteStyles = StyleSheet.create({
    text: {
        ...TextStyles.text,
        fontSize: 18,
        textAlign: 'center',
        marginTop: 24,
        marginBottom: 20
    }
})

export default ModalDeleteStyles