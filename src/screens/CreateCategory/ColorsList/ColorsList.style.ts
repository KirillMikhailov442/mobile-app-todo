import { StyleSheet, Dimensions } from 'react-native';

const ColorsListStyles = StyleSheet.create({
    container: {
        height: 70,
        alignItems: 'center',
    }
})

export default ColorsListStyles