import { StyleSheet } from "react-native";

import { textColors } from "../../constants/colors";


const PreferencesLayoutStyles = StyleSheet.create({
    section: {
        marginTop: 16
    },
    sectionTitle: {
        color: textColors.grayDefault,
        fontSize: 14
    },
})

export default PreferencesLayoutStyles